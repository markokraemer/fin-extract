# Minimal dependencies for financial-extractor
# Core document processing (includes pandas, openpyxl, pydantic, easyocr, python-dotenv, etc.)
docling==2.48.0

# LLM integration
openai==1.102.0
langchain-openai==0.3.32
